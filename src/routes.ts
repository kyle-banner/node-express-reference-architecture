// import { Router } from 'express';
// import employees from './services/employees/employees.mockData';
// import EmployeesController from './services/employees/employees.controller';
// import EmployeesService from './services/employees/employees.service';
// const routes = Router();

// routes.get('/employees', (req, res) => {
//   // TODO: don't new up service and container here,
//   // initialize on startup as in project-tanner-smiths
//   const employeesService = new EmployeesService();
//   const employeesController = new EmployeesController(employeesService);
//   employeesController.getEmployees();
//   // res.status(200).json(employees);
// });

// export default routes;
